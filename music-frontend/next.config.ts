import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  images: {
    domains: [
      "localhost", 
      // "meagerly-amused-molly.cloudpub.ru",
    ], // Add your domains here
  },
};

export default nextConfig;
